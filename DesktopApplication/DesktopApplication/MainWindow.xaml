<Window x:Class="DesktopApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Background" Value="#7D99AA" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#66F4FF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"  Grid.Column="1"
                   Text="User Management System for Feast AND Find" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="10" />

        <DataGrid x:Name="UserGrid" Grid.Row="1" Grid.Column="1" 
                  AutoGenerateColumns="False" Margin="10"
                  VerticalAlignment="Stretch" 
                  HorizontalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding userid}" Width="auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding username}" />
                <DataGridTextColumn Header="Email" Binding="{Binding email}"/>
                <DataGridTextColumn Header="Password" Binding="{Binding password}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Grid.Column="0"
            VerticalAlignment="Top" 
            HorizontalAlignment="Center" Margin="10">
            <Button Content="Add User" Width="150" Click="AddButton_Click" />
            <Button Content="Modify User" Width="150" Click="ModifyButton_Click" />
            <Button Content="Delete User" Width="150" Click="DeleteButton_Click" />
            <Button Content="Refresh Data" Click="RefreshButton_Click" />
        </StackPanel>
        
        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="Feast AND Find" 
                   FontSize="16" FontStyle="Oblique" 
                   HorizontalAlignment="Center" Margin="10" Background="Bisque"/>
    </Grid>
</Window>
